const initialState = {
    numOfCakes : 10
}

//inicializamos el estado conun vaor inicial
const cakeReducer = ( state = initialState,action ) => {
    switch (action.type) {
        case 'BUY_CAKE':
            
            break;
    
        default:
            break;
    }
}